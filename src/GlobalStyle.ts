import { createGlobalStyle } from 'styled-components';

export const GlobalStyle = createGlobalStyle`
  body {
    box-sizing: border-box;
    margin: 0;

    font-family: Pretendard, apple-system, BlinkMacSystemFont, 나눔스퀘어, 'Helvetica Neue', Helvetica, Arial, sans-serif, 'East Sea Dokdo';

    overflow: hidden;
    background: black;
  }
`;
